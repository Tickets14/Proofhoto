// Widget tests require a fully-initialized Hive instance.
// For now this is a placeholder — run on a device for real testing.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
